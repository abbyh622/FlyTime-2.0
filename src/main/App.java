package main;

public class App {

    public static Experiment experiment; 
    public static Integer[] behaviorNums; 
    
    public static void main(String[] args) throws Exception {
        // have experiment name, behavior maps, and behavior nums as static after selecting experiment 
        // behaviorNums = experiment.getBehaviors().keySet().toArray(new Integer[0]);
    }
}
